{
  "name": "ga4-datalayer-as-code",
  "version": "5.0.0",
  "private": true,
  "description": "Strict GA4 web dataLayer contract as composable JSON Schema files for implementation, validation, monitoring, and AI-ready tracking governance.",
  "license": "MIT",
  "scripts": {
    "validate:schemas": "node scripts/validate-schemas.js",
    "validate:examples": "node scripts/validate-examples.js",
    "validate": "npm run validate:schemas && npm run validate:examples",
    "generate:index": "node scripts/generate-index.js",
    "generate:docs": "node scripts/generate-docs.js",
    "generate": "npm run generate:index && npm run generate:docs"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "engines": {
    "node": ">=18"
  }
}
